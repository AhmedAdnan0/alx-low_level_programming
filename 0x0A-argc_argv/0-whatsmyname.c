#include <stdio.h>

/**
 * main - prints its name
 * @argc: argument count
 * @argv: argument value
 *
 * Return: 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
